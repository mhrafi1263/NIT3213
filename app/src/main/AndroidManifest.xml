<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed for Retrofit/HTTP -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".NIT3213App"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NIT3213">

        <!-- Details (has back/up to Dashboard) -->
        <activity
            android:name=".ui.DetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.DashboardActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.DashboardActivity" />
        </activity>

        <!-- Dashboard (opened after login) -->
        <activity
            android:name=".ui.DashboardActivity"
            android:exported="false" />

        <!-- Login is the launcher -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
